{
  "name": "example-esbuild",
  "version": "0.0.1",
  "scripts": {
    "build": "esbuild main.js --format=esm --loader:.wasm=file --bundle --outdir=dist && cp index.html dist/"
  },
  "devDependencies": {
    "esbuild": "0.23.1"
  },
  "dependencies": {
    "evmole": "file:../../evmole.latest.tgz"
  }
}
